'use client';

import UsoMetodos from './UsoMetodos';

export default function Page() {
    return (
        <main style={{ padding: '2rem' }}>
            <h1>Gestión de Habitaciones</h1>
            <UsoMetodos />
        </main>
    );
}
